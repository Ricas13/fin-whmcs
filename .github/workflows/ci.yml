name: CI

on:
  push:
    branches:
      - main
      - 'feat/**'
      - 'fix/**'
  pull_request:

permissions:
  contents: read

jobs:
  test:
    name: PHP ${{ matrix.php }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        php: ['8.1', '8.2', '8.3']

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          coverage: none
          extensions: curl,json,mbstring
          tools: composer:v2

      - name: Validate Composer manifest
        run: composer validate --strict

      - name: Install development dependencies
        run: composer install --no-interaction --prefer-dist

      - name: Lint module PHP
        run: find modules tests scripts -name '*.php' -print0 | xargs -0 -n1 php -l

      - name: Validate Marketplace package layout
        run: php scripts/validate-package.php

      - name: Run contract tests
        run: vendor/bin/phpunit

  jellyfin-runtime:
    name: Jellyfin 10.11 runtime smoke
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          coverage: none
          extensions: curl,json,mbstring
          tools: composer:v2

      - name: Install development dependencies
        run: composer install --no-interaction --prefer-dist

      - name: Run disposable Jellyfin runtime smoke
        run: bash scripts/test-runtime.sh
