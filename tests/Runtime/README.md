# Runtime Integration Tests

These tests exercise the real exported CAPTAiNFiN WHMCS module entrypoints against real external service containers where practical.

They are intentionally separate from source/contract tests. A feature is not considered runtime-proven merely because a PHP function or route exists.
